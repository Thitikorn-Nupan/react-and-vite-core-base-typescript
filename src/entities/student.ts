export class Student {

    public sid : number
    public fullname : string
    public age : number

    constructor(sid: number, fullname: string, age: number) {
        this.sid = sid;
        this.fullname = fullname;
        this.age = age;
    }
}